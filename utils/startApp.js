import domBuilder from '../shared/domBuilder';
import navBar from '../shared/navBar';

const startApp = (user) => {
  domBuilder(user);
  navBar(user);
  // formEvents
  // navBar
  // logoutButton
  // navigationEvents
};

export default startApp;
